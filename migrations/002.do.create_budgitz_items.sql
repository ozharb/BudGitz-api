CREATE TABLE budgitz_items (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    item_name TEXT NOT NULL,
    price NUMERIC NOT NULL,
    content TEXT,
    date_made TIMESTAMPTZ DEFAULT now() NOT NULL,
    list_id INTEGER REFERENCES budgitz_lists(id) ON DELETE CASCADE NOT NULL
);